# MCP Server Configurations
# Add your MCP servers here

mcp_servers:
  - name: "research"
    command: "python"
    args: ["src/backend/mcp_servers/mcp_research/server.py"]

  # THIS IS THE FORMATTING SERVER
  - name: "formatting"
    command: "python"
    args: ["src/backend/mcp_servers/mcp_formatting/server.py"]

  - name: "chart-generation"
    command: "python"
    args: ["src/backend/mcp_servers/mcp_chart_generation/server.py"]
    env:
      FASTMCP_CHECK_FOR_UPDATES: "never"

  # Example server configuration:
  # - name: "example-server"
  #   command: "python"
  #   args: ["-m", "example_mcp_server"]
  #   env:
  #     SOME_VAR: "value"

  # Filesystem server example:
  # - name: "filesystem"
  #   command: "npx"
  #   args: ["-y", "@modelcontextprotocol/server-filesystem", "/path/to/allowed/directory"]
